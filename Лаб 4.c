#include <stdio.h>
#include <math.h>
int main()
{
  double a, b, c;
  printf("Введите стороны треугольника: ");
  scanf("%lf %lf %lf", &a, &b, &c);
  if (a < b + c && b < a + c && c < a + b)
  {
    printf("%d", )
  }
    
  return 0;
}
